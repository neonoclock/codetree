const fs = require('fs');
let input = Number(fs.readFileSync(0).toString().trim('/n'));

let a = input[0];
let b = input[1];
let c = input[2];

console.log(`${a.toFixed(3)}`);
console.log(`${a.toFixed(3)}`);